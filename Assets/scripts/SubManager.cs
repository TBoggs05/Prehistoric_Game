using Unity.VisualScripting;
using UnityEngine;
using UnityEngine.SceneManagement;

// Child of GameManager to add additional functions for the main scene, such as pausing, etc.
public class SubManager : GameManager
{
    // Update is called once per frame
    void Update()
    {
        
    }
}
